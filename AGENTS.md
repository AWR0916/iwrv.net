# Repository Instructions

## AI開発標準

本リポジトリの開発は、AI開発標準 `v1.0.0` を基準とします。

[AI開発標準 v1.0.0](https://github.com/AWR0916/ai-development-standard/tree/v1.0.0)

プロジェクト固有規則とAI開発標準が同じ対象に適用される場合は、原則として厳しい規則を適用します。

標準からの例外には、対象規則、理由、影響、代替策、承認者、有効期限を記録します。

## Project scope

藍・W・楽歌（Ai Winston Rucker）の公式サイト <https://iwrv.net/> を管理します。ビルド工程のない静的サイトで、`main` ブランチのルートからGitHub Pagesへ自動公開されます。

目的は、YouTubeチャンネル登録、Raidoriファンクラブ登録、企業案件・イベント出演の問い合わせ獲得です。

対象外:

- サイト表示に使わないソースコード、資格情報、問い合わせ者の個人情報
- 他リポジトリ向けの指示
- ビルドツールやフレームワークの導入（静的構成を維持します）

背景、デザイン方針、過去の実装判断は [CODEX_HANDOFF.md](CODEX_HANDOFF.md) に記載しています。本ファイルと矛盾する場合は本ファイルを優先し、差分を CODEX_HANDOFF.md へ反映します。

## Project-specific rules

### 先祖返り防止（最優先）

このリポジトリは複数のAI（Claude、Codexなど）と利用者が並行して編集します。他の作業を消す「先祖返り」を最も重大な事故として扱います。2026-09-20に、古い手元コピーで `index.html` を上書きし、旅行記・プロフィールへの導線と記事カードが消える事故が起きました。

- 編集の直前に、フォルダにある最新のファイルを読み直します。以前に読んだ内容や手元のコピーを土台にしません。
- 書き戻すのは依頼された箇所だけです。ファイル全体を作り直して置き換えません。
- 書き戻す前に「現在のファイル → 書き戻す内容」の差分を確認し、依頼外の行が消えたり変わったりしていないことを確かめます。想定外の差分があれば書き戻さず、利用者に報告します。
- 読み直した後に他の作業でファイルが更新されていたら、上書きせずに最新版を読み直して反映し直します。強制上書きは行いません。
- 依頼されていないファイル（CSS、JavaScript、他のページなど）は書き戻しません。
- 作業前に `git log` またはコミット履歴で直近の変更を確認し、自分以外の変更が残っていることを反映前にも確かめます。

### 必須

- 変更前に現在の `index.html` を確認します。HTML、CSS、JavaScriptはこのファイルに内包されています。
- スマホ表示を最優先します。幅390pxで表示崩れ、重なり、横方向のはみ出しが無いことを確認します。
- 画像パスは大文字・小文字を区別して記述します。GitHub Pagesでは区別されます（例: `images/ai-winston-rucker-main.PNG`）。
- 依頼された範囲以外のリファクタリングを行いません。
- 利用者が加えた変更を戻しません。`force push`、`reset --hard`、既存変更の巻き戻しを行いません。
- 週次スケジュール画像は同じファイル名で上書きしません。週ごとに新しいファイル名を付けます。ブラウザキャッシュで古い画像が残るのを防ぐためです。
- 表示データは `data/onair.json` と `data/updates.json` を編集します。HTMLへ直接書き込みません。
- `data/updates.json` には閲覧者に関係する更新だけを記載し、最大5件に保ちます。Analytics設定や内部的な文言調整は記載しません。
- リポジトリ直下の `ai-winston-rucker-main.PNG` は過去から残る重複ファイルです。必要性を確認せずに削除しません。
- X APIと投稿埋め込み、VRoid Hubのiframe、Instagramを使用しません。いずれも不採用または撤去済みです。

### 推奨

- 既存のデザイン方針と文章のトーンを維持します。黒、アイボリー、金、控えめな紫。明朝体と紙面風の構成。
- 変更内容と変更したファイルを箇条書きで報告します。
- 週次スケジュールの更新は [SCHEDULE_UPDATE.md](SCHEDULE_UPDATE.md) および [SCHEDULE_UPDATE_v2.md](SCHEDULE_UPDATE_v2.md) の手順に従います。

## Required checks

`main` へ反映する前に、次をすべて確認します。

- HTMLの主要タグの開始数と終了数が一致すること
- `data/onair.json` と `data/updates.json` がJSONとして妥当であること
- 追加・変更した画像パスが実在し、大文字・小文字まで一致すること
- リンク切れと、未設定の `href="#"` が残っていないこと
- 幅390pxでの表示に崩れ、重なり、横方向のはみ出しが無いこと
- `git status` で反映対象を確認したこと
- 反映対象の各ファイルについて、直前のコミットとの差分が依頼した範囲だけであること（他の作業を消していないこと）

反映後、<https://iwrv.net/> で表示を確認します。GitHub Pagesのデプロイに数十秒かかる場合があります。

## 例外

| 項目 | 内容 |
| --- | --- |
| 対象となる標準と規則 | 導入ガイド「文書検査の導入」 — `reusable-documentation-check.yml@v1.0.0` の追加 |
| 適用できない理由 | 本リポジトリは未導入。導入可否を判断していないため |
| 影響 | Markdown文書の自動検査が働かず、書式と相互リンクの逸脱を人手で確認する必要がある |
| 代替策と確認方法 | 上記「Required checks」を変更ごとに手で実施する |
| 承認者 | （未記入） |
| 有効期限と再評価日 | （未記入） |

この例外は、ワークフローを導入した時点で削除します。
