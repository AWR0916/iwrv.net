# 週次スケジュールの更新手順

最終更新: 2026-08-30 ／ 新デザイン（週刊番組誌）用の手順

> **この文書が現行の手順です。** 2026-08-30 にトップページを新デザインへ差し替えました。旧 `SCHEDULE_UPDATE.md` は案内だけを残した抜け殻なので、削除して構いません。

---

## いちばん短い説明

**画像の差し替えは不要になりました。`data/schedule.json` を書き換えるだけです。**

トップページの番組表は、画像ではなくテキストから組み立てています。JSONを直せば、PCの7列表とスマホのリストが両方いっぺんに変わります。

---

## 手順

### 1. `data/schedule.json` を開く

```json
{
  "period": "2026年8月4週",
  "range": "2026.08.24 — 08.30",
  "days": [
    { "date": "24", "dow": "MON", "time": "21:00", "title": "AlternativeRadio #25", "genre": "雑学・音楽・近況", "kind": "radio" },
    { "date": "27", "dow": "THU", "off": "おやすみ" }
  ]
}
```

### 2. 番組表を書き換える

| キー | 意味 | 書き方 |
| --- | --- | --- |
| `range` | 見出しの横に出る期間 | `2026.08.31 — 09.06` |
| `date` | 日にち | `31` |
| `dow` | 曜日 | `MON` `TUE` `WED` `THU` `FRI` `SAT` `SUN` |
| `time` | 開始時刻 | `21:00` |
| `title` | 番組名 | `AlternativeRadio #26` |
| `genre` | 小さく出るジャンル名 | `雑学・音楽・近況` |
| `kind` | 左の色棒の色 | `radio`（紫・ラジオ）／`sing`（ローズ・歌枠）／`act`（青・声劇）／`game`（緑・ゲーム/Vlog）／`sp`（金・特番） |
| `off` | 放送がない日 | `"off": "おやすみ"` または `"off": "未定"` |

**放送がない日は `time` `title` `genre` `kind` を書かず、`off` だけにします。**

`days` は必ず7日ぶん、月曜から日曜の順で並べてください。

### 3. 確認する

```powershell
cd D:\Document\claudecode\Rucker_website
python -m http.server 8000
```

`http://localhost:8000/` を開いて番組表を確認します。**表示が変わらないときは、JSONの書式ミスでほぼ確実です**（カンマの付け忘れ、閉じ括弧の抜け）。その場合ページはHTMLに書いてある前の週の内容を表示し続けます。ブラウザのコンソール（F12）にエラーが出ます。

### 4. 更新情報を1行足す

`data/updates.json` の先頭に追加します。新しい順、最大5件。

```json
{ "date": "2026-08-31", "displayDate": "2026.08.31", "text": "8月5週の配信スケジュールを公開", "href": "#programme" }
```

### 5. commit / push

```powershell
git add data/schedule.json data/updates.json
git commit -m "8月5週の番組表を更新"
git push origin main
```

反映まで数十秒かかります。`https://iwrv.net/` で確認してください。

---

## よくある質問

**Q. スケジュール画像はもう作らなくていい？**

サイトに載せるためだけなら不要です。ただしXで告知するときは画像のほうが目に留まるので、**X用には引き続き作る価値があります**。サイトとX用画像は別物、という整理になりました。

**Q. 画像だけ渡して、JSONを作ってもらうことはできる？**

できます。スケジュール画像かテキストを渡していただければ、JSONに起こします。書式ミスの心配がないぶん、こちらのほうが安全です。

**Q. JSONを直したのに反映されない**

以下の順で確認してください。

1. ブラウザのキャッシュ（Ctrl+F5で再読み込み）
2. JSONの書式（[JSONLint](https://jsonlint.com/) に貼ると一発で分かります）
3. `git push` したか、GitHub Pagesの反映を待ったか

**Q. HTMLに書いてある番組表は何？**

`index.html` の `<div class="week">` と `<ul class="week-list">` は、JSONの取得に失敗したときのための予備表示です。通常はJSONで上書きされます。**JSONを更新するとき、余裕があればここも同じ内容に直しておく**と、万一の通信失敗時に古い週が出るのを防げます（必須ではありません）。

---

## 旧手順との違い

| | 旧 | 新 |
| --- | --- | --- |
| 番組表の中身 | 画像1枚（`images/onair/YYYY-MM-week-N.png`） | `data/schedule.json` のテキスト |
| 更新作業 | 画像作成 → 追加 → `onair.json` 編集 → commit | JSON編集 → commit |
| 検索エンジン | 画像なので読めない | テキストなので読める |
| 読み上げ | 内容が読めない | 読める |

`data/onair.json` と `images/onair/` は旧デザイン用です。新トップに切り替えたあとは使いませんが、**バックナンバーとして残しておいて構いません**（消す必要はありません）。
